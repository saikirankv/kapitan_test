parameters:
  namespace: ${target}
  kapitan:
    vars:
      target: ${target}
    compile:
      - output_path: manifests
        output_type: json
        input_type: jsonnet
        input_paths:
          - components/configurations.jsonnet